# CheckOn Frontend 작업 규칙

- React와 JavaScript/JSX만 사용한다.
- TypeScript 파일과 TypeScript 문법은 사용하지 않는다.
- 페이지는 기능별 폴더로 분리한다.
- 각 페이지 폴더는 JSX 파일, CSS 파일, components 폴더 구조를 따른다.
- App.jsx에 모든 UI와 로직을 작성하지 않는다.
- 서비스명과 브랜딩 문구는 CheckOn으로 통일한다.
- 기존 UI 스타일과 사용자 흐름을 최대한 유지한다.
- 요청하지 않은 기능이나 API 호출은 구현하지 않는다.
- 실제 구현이 필요한 부분은 TODO 주석으로 표시한다.
- 사용하지 않는 페이지, 컴포넌트, 메뉴, 입력 항목은 제거한다.
- 불필요한 여백과 UI 요소를 정리한다.
- 로고와 주요 버튼은 요청된 페이지 이동 경로에 맞게 연결한다.
- 존재하지 않는 파일이나 API를 import하지 않는다.
- 파일을 수정하기 전에 기존 코드와 폴더 구조를 확인한다.
- 수정 후 반드시 `pnpm run build`를 실행한다.
- 빌드 오류가 있으면 원인을 확인하고 수정한 뒤 다시 검증한다.
